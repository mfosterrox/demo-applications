apiVersion: apps/v1
kind: Deployment
metadata:
  name: shop-web
  namespace: demo-dev
  labels:
    app: shop
    component: web
    app.version: "1.0.0"
    team: team-a
    demo: vulnmgmt
spec:
  replicas: 1
  selector:
    matchLabels:
      app: shop
      component: web
  template:
    metadata:
      labels:
        app: shop
        component: web
        app.version: "1.0.0"
        team: team-a
        demo: vulnmgmt
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: shop-web
          image: quay.io/mfoster/shop-web:1.0.0
          imagePullPolicy: IfNotPresent
          command: ["sleep", "infinity"]
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            capabilities:
              drop:
                - ALL
            seccompProfile:
              type: RuntimeDefault
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              cpu: 100m
              memory: 128Mi
      volumes:
        - name: tmp
          emptyDir: {}
